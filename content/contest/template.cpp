#include <bits/stdc++.h>
using namespace std;

// No decorrer do notebook, podem aparecer alguns dos seguintes comandos, para diminuir a quantidade de caracteres e facilitar copiar o codigo (nao eh necessario copiar isso, apenas entender caso apareca algum assim la na frente)
#define rep(i, a, b) for(int i = a; i < (b); ++i)
#define all(x) begin(x), end(x)
#define sz(x) (int)(x).size()
typedef long long ll;
typedef pair<int, int> pii;
typedef vector<int> vi;

int main() {
	ios::sync_with_stdio(false); cin.tie(nullptr);
	int x;
	cin >> x;
	cout << x << "\n";
}
